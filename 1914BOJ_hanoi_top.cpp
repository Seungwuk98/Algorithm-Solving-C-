#include <bits/stdc++.h>

using namespace std;

void sol(vector<pii> &ret){
    
}



int main()
{
    cin >> n;
    int a,b;
    result = pow(2, n)-1;
    if (n>20) {
        cout << result;
        return 0;
    } 
    for (int i=n; i--;) t[0].push_back(i);
    vector<pii> ret;
    sol(ret);
    cout << result << endl;
    for (auto &p : ret) {
        cout << p.first << ' ' << p.second << '\n';
    }


} // namespace std'
